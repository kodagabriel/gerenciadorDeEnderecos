# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GerenciadorDeEnderecos' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GerenciadorDeEnderecos
  pod 'SwiftLint'
  target 'GerenciadorDeEnderecosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GerenciadorDeEnderecosUITests' do
    # Pods for testing
  end

end
